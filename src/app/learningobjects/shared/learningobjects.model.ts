// class were I defined the string values where they were be called later in order to be displayed on the website as well as 
// being defined by what the user inputs 
export class Learningobject {
    $key: string;
     name: string;
    // tslint:disable-next-line:quotemark
   // level = ["K-12", "Undergraduate", "Graduate"];
   level: string;
}
